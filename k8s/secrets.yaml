apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: task-management
type: Opaque
stringData:
  DB_PASSWORD: "changeme_taskpassword"
  DB_ROOT_PASSWORD: "changeme_rootpassword"
  KEYCLOAK_ADMIN_PASSWORD: "changeme_keycloak_admin"
  SMTP_USER: "your_smtp_username"
  SMTP_PASS: "your_smtp_password"
